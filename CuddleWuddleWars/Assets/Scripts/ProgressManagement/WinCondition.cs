using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WinCondition : MonoBehaviour
{
   /* public GameObject winCanvas;
    private WinManager winManager;
    public WinDisplay winDisplay;

    void Start()
    {
        winManager = FindObjectOfType<WinManager>(); // Find the WinManager script in the scene
    }

    // Call this method when the win condition is met
    public void CheckWinCondition()
    {
        if (winCanvas.activeSelf) // Check if the winCanvas is active
        {
            winManager.SetGameWon(); // Call the SetGameWon method of the WinManager
            winDisplay.UpdateWinText(); // Update the win display text
        }
    }
   */
}
